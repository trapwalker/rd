var Toolbar = (function () {
    function Toolbar(options) {
        this.options = {
            contentDiv: '',
            checkTypeMarkers: true,
        };
        setOptions(options, this.options);

        // для быстрого обращения
        var contentDiv = $('#'+this.options.contentDiv);

        //



    }

    //
    Toolbar.prototype.hideWindow = function () {

    };


    return Toolbar;
})();
