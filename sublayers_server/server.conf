# -*- coding: utf-8 -*-

pidfile = "sl_engine.pid"
debug = True
logging = "none"
# cookie_secret intentionally not set here - comes from the COOKIE_SECRET
# env var (see settings.py), or set it in local.server.conf for a non-Docker
# dev run. Must match sublayers_site/site_server.conf's value: both services
# read/write the same secure "user" cookie.
map_link = "/map"

port = 8000
ws_port = 8000
