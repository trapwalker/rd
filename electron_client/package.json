{
  "name": "RoadDogs",
  "version": "1.0.0",
  "description": "Road Dogs Online Game Client",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager .  RoadDogs --overwrite --platform=win32 --arch=x64 --out=build",
    "rebuild": "electron-rebuild -f -w greenworks"
  },
  "dependencies": {
    "electron-config": "^1.0.0",
    "greenworks": "git+https://github.com/greenheartgames/greenworks.git",
    "jquery": "^3.2.1"
  },
  "devDependencies": {
    "electron": "^1.7.6",
    "electron-config": "^1.0.0",
    "electron-rebuild": "^1.6.0"
  }
}
